# Server Configuration
server.port=3015

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/astendor
spring.datasource.username=warlord
spring.datasource.password=dragonslayer
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=astendor-secret-key-for-jwt-token-generation-make-this-very-long-and-secure
jwt.expiration=86400000
